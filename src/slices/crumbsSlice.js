import {createSlice} from "@reduxjs/toolkit";

const initialState = []

const crumbsSlice = createSlice({
  name: 'CrumbsSlice',
  initialState,
  reducers: {
    addCrumbsSlice: (state, action) => {

    }
  }
})